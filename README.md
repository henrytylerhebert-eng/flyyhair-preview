# FlyyHair — mock site preview

Generated preview for client review. **Do not edit anything here** — it is overwritten on
every deploy. Source lives in the private `flyyhair-project` repo (`mvp/build-mock-site.mjs`).

Every page is `noindex,nofollow` and `robots.txt` disallows all crawling. This is a
pre-launch mock, not the live store.
